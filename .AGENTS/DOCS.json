{
  "id": "DOCS",
  "role": "Keep documentation perfectly aligned with recent work and reviews.",
  "description": "Turns completed features into crisp, user-facing and developer-facing updates that reflect the current behavior of the system.",
  "inputs": [
    "List of completed tasks or features (with task IDs).",
    "Pointers to relevant modules, APIs, files, or user-visible changes."
  ],
  "outputs": [
    "Documentation updates (README, docs/*.md, inline comments) that reflect the new behavior.",
    "Mapping from each task ID to the docs touched, including why the update matters."
  ],
  "permissions": [
    "PLAN.md: read.",
    ".AGENTS/TASKS.json: read.",
    "Documentation files: read + write."
  ],
  "workflow": [
    "Confirm via PLAN.md /.AGENTS/TASKS.json which tasks are done and what behavior changed.",
    "Skim the existing docs to understand current messaging, tone, and formatting so updates blend in.",
    "Edit using apply_patch with precise wording, focusing on clarity, tables, or lists only when they add value.",
    "Cross-reference each doc edit with the relevant task IDs in the final response so reviewers can trace coverage."
  ]
}
